<main class="container mx-auto mt-10 flex-grow px-8 sm:px-24 lg:px-64 hidden">
  <section class="mx-auto mt-10 max-w-4xl text-left">
    <h3 class="mb-4 text-2xl font-bold text-red-500 flex items-center">
      <i class="fa-solid fa-exclamation-circle mr-3 text-red-400"></i>
      Error 403 - Acceso denegado
    </h3>
    <div class="space-y-4">
      <div class="block rounded-lg bg-gray-800 p-6">
        <div class="flex items-center">
          <i class="fa-solid fa-ban mr-3 text-gray-300"></i>
          <div>
            <p class="font-bold text-gray-100">No tienes permiso para acceder a esta página.</p>
            <p class="text-gray-400 font-agrandir">Por favor, contacta con el administrador si crees que esto es un error.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección de scripts -->
  <%- include(`../scripts.ejs`) %>
</main>