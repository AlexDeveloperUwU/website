<main class="container mx-auto mt-10 flex-grow px-8 sm:px-24 lg:px-64 hidden">
  <section class="mx-auto mt-10 max-w-4xl text-left">
    <h3 class="mb-4 text-2xl font-bold text-red-500 flex items-center">
      <i class="fa-solid fa-magnifying-glass-minus mr-3 text-red-400"></i>
      Error 404 - Página no encontrada
    </h3>
    <div class="space-y-6">
      <div class="block rounded-lg bg-gray-800 p-6">
        <div class="flex items-center">
          <i class="fa-solid fa-exclamation-triangle mr-3 text-gray-300"></i>
          <div>
            <p class="font-bold text-gray-100">Lo sentimos, la página que estás buscando no existe.</p>
            <p class="text-gray-400 font-agrandir">Puede que haya sido movida o eliminada, o tal vez la URL que ingresaste sea incorrecta.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección de scripts -->
  <%- include(`../scripts.ejs`) %>
</main>